name: Pull Request

on: 
  workflow_dispatch:
  pull_request:
  
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - run: echo "🏗️ Building..."
  
  deploy-qa:
    name: Deploy QA
    runs-on: ubuntu-latest
    needs: build
    environment: QA
    steps:
      - run: echo "🕵️ Deployin QA..."
  
